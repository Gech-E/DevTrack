# Copied from root for local Nest ConfigModule loading.
# Prefer editing root .env.example; keep these in sync during Phase 1.

API_PORT=3001
DATABASE_URL=postgresql://devtrack:devtrack@localhost:55432/devtrack?schema=public
JWT_SECRET=change-me-in-development-only
