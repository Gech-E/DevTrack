/**
 * Shared, framework-free contracts for DevTrack.
 * Add DTOs, enums, and constants here in later phases — keep Nest/Next out.
 */
export const APP_NAME = 'DevTrack' as const;

export type HealthStatus = 'ok';
